#pragma once

#include <poker_eval/equity/RangeEquity.h>

/*
 * En-tête de compatibilité conservé pour l’ancien nom RangeEquity_MT.h.
 * Toutes les déclarations publiques (CalculateEquityForRanges_MT, v2, v3, v4, v5,
 * version batched, etc.) résident désormais dans RangeEquity.h.
 * 
 * Inclure ce fichier vous donne simplement accès au nouvel en-tête officiel.
 */
